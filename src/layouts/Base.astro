---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
		<title>{title}</title>
		<style is:global>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			html {
				font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
				font-size: 16px;
				line-height: 1.6;
				color: #1a1a1a;
				background: #fff;
			}

			body {
				min-height: 100vh;
				background: #f9f9f9;
			}

			a {
				color: inherit;
				text-decoration: underline;
				text-underline-offset: 2px;
			}

			a:hover {
				color: #666;
			}

			.frame {
				max-width: 1078px;
				margin: 0 auto;
				border-left: 1px solid #e5e5e5;
				border-right: 1px solid #e5e5e5;
				min-height: 100vh;
				display: flex;
				flex-direction: column;
				background: #fff;
			}

			.container {
				padding: 0 2rem;
			}

			/* Header */
			header {
				border-bottom: 1px solid #e5e5e5;
				padding: 1rem 0;
			}

			header .container {
				display: flex;
				justify-content: space-between;
				align-items: center;
			}

			.logo {
				font-weight: 600;
				font-size: 1.1rem;
				text-decoration: none;
			}

			nav {
				display: flex;
				gap: 2rem;
				align-items: center;
			}

			nav a {
				text-decoration: none;
				color: #666;
				font-size: 0.95rem;
			}

			nav a:hover {
				color: #000;
			}

			/* Main */
			main {
				padding: 4rem 0;
				flex: 1;
			}

			/* Footer */
			footer {
				border-top: 1px solid #e5e5e5;
				padding: 2rem 0;
			}

			footer .container {
				display: flex;
				justify-content: space-between;
				align-items: center;
				color: #666;
				font-size: 0.9rem;
			}

			footer nav {
				gap: 1.5rem;
			}

			code {
				font-family: 'JetBrains Mono', monospace;
				font-size: 0.9em;
				background: #f5f5f5;
				padding: 0.2em 0.4em;
				border-radius: 3px;
			}

			pre {
				font-family: 'JetBrains Mono', monospace;
				background: #1a1a1a;
				color: #e5e5e5;
				padding: 1rem;
				border-radius: 6px;
				overflow-x: auto;
			}

			pre code {
				background: none;
				padding: 0;
			}
		</style>
	</head>
	<body>
		<div class="frame">
			<header>
				<div class="container">
					<a href="/" class="logo">Strannik</a>
					<nav>
						<a href="https://github.com/ether-strannik" target="_blank">GitHub</a>
						<a href="/docs">Docs</a>
					</nav>
				</div>
			</header>

			<main>
				<div class="container">
					<slot />
				</div>
			</main>

			<footer>
				<div class="container">
					<span>&copy; 2026</span>
					<nav>
						<a href="https://github.com/ether-strannik" target="_blank">GitHub</a>
					</nav>
				</div>
			</footer>
		</div>
	</body>
</html>
