---
import Base from '../layouts/Base.astro';
---

<Base title="Strannik">
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/iosevka@5.0.4/index.css" />
	<style>
		:global(body) {
			font-family: 'Iosevka', monospace;
		}
		:global(.logo) {
			font-family: 'Iosevka', monospace;
		}
		.hero {
			padding: 0.5rem 0;
		}

		.hero h1 {
			font-size: 2.2rem;
			font-weight: 600;
			margin-bottom: 1rem;
			letter-spacing: -0.02em;
		}

		.hero p {
			color: #666;
			font-size: 1.4rem;
			margin-bottom: 2.5rem;
			white-space: nowrap;
		}

		.actions {
			display: flex;
			gap: 1rem;
			align-items: center;
			margin-bottom: 3rem;
		}

		.btn {
			display: inline-block;
			padding: 0.7rem 1.4rem;
			font-size: 0.95rem;
			text-decoration: none;
			border-radius: 4px;
		}

		.btn-primary {
			background: #000;
			color: #fff;
		}

		.btn-primary:hover {
			background: #333;
			color: #fff;
		}

		.btn-secondary {
			color: #666;
		}

		/* Tabs */
		.tabs {
			border-bottom: 1px solid #e5e5e5;
			margin-bottom: 2rem;
		}

		.tab-list {
			display: flex;
			gap: 2rem;
			list-style: none;
		}

		.tab-btn {
			background: none;
			border: none;
			padding: 0.5rem 0;
			font-size: 0.95rem;
			font-family: inherit;
			color: #666;
			cursor: pointer;
			position: relative;
		}

		.tab-btn:hover {
			color: #000;
		}

		.tab-btn.active {
			color: #000;
		}

		.tab-btn.active::after {
			content: '';
			position: absolute;
			bottom: -1px;
			left: 0;
			right: 0;
			height: 2px;
			background: #000;
		}

		/* Tab content */
		.tab-content {
			display: none;
		}

		.tab-content.active {
			display: block;
		}

		.app-header {
			display: flex;
			gap: 1.5rem;
			align-items: flex-start;
			margin-bottom: 1.5rem;
		}

		.app-icon {
			width: 95px;
			height: 95px;
		}

		.app-title {
			font-size: 1.1rem;
			font-weight: 600;
			margin-bottom: 0.5rem;
		}

		.app-desc {
			color: #666;
			margin-bottom: 0;
		}

		.feature-list {
			list-style: none;
			margin-bottom: 2rem;
		}

		.feature-list li {
			padding: 0.4rem 0;
			color: #333;
		}

		.feature-list li::before {
			content: '[*]';
			color: #999;
			margin-right: 0.75rem;
			font-size: 0.85rem;
		}

		.feature-list strong {
			color: #000;
			margin-right: 1rem;
		}

		.btn-docs {
			display: inline-flex;
			align-items: center;
			gap: 0.5rem;
			background: #000;
			color: #fff;
			padding: 0.6rem 1.2rem;
			font-size: 0.9rem;
			text-decoration: none;
			border-radius: 4px;
		}

		.btn-docs:hover {
			background: #333;
			color: #fff;
		}

		.btn-row {
			display: flex;
			gap: 1rem;
			align-items: center;
		}

		.btn-github {
			display: inline-flex;
			align-items: center;
			padding: 0.5rem 1rem;
			border: 1px solid #e5e5e5;
			border-radius: 4px;
			color: #666;
			text-decoration: none;
			font-size: 0.9rem;
		}

		.btn-github:hover {
			color: #000;
			border-color: #ccc;
		}

	</style>

	<div class="hero">
		<p>Open source software, built in the open.</p>
	</div>

	<div class="tabs">
		<ul class="tab-list">
			<li><button class="tab-btn active" data-tab="prismchart">PrismChart</button></li>
			<li><button class="tab-btn" data-tab="tab">Tab</button></li>
		</ul>
	</div>

	<div class="tab-content active" id="prismchart">
		<div class="app-header">
			<img src="/icons/prismchart.png" alt="PrismChart" class="app-icon" />
			<div>
				<h3 class="app-title">PrismChart</h3>
				<p class="app-desc">An open source charting toolkit that extends MetaTrader 5 with drawing and analysis tools. Draw, organize, document, review.</p>
			</div>
		</div>

		<ul class="feature-list">
			<li><strong>Custom Scales</strong> Canvas-rendered price and time axes with timezone support</li>
			<li><strong>Rich Drawings</strong> Rectangles with text, lines with labels - objects that stay connected</li>
			<li><strong>Layer System</strong> Organize by groups with visibility/lock controls and switchable visibility templates</li>
			<li><strong>Alerts</strong> Price cross, zone entry, candle close alerts with enhanced SMTP email notifications</li>
			<li><strong>Portability</strong> Copy/paste, import/export drawings between charts - all or selected</li>
			<li><strong>Trade Journal</strong> ThinkPad app with notes, screenshots, calendar, and statistics</li>
			<li><strong>Time Context</strong> Sessions, news events, period separators, and other technical analysis overlays</li>
		</ul>

		<div class="btn-row">
			<a href="/docs/prismchart" class="btn-docs">Docs</a>
			<a href="/download/prismchart" class="btn-docs">
				<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
					<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M7 10l5 5 5-5M12 15V3"/>
				</svg>
				Download
			</a>
			<a href="https://github.com/ether-strannik/PrismChart" class="btn-github" target="_blank">GitHub</a>
		</div>
	</div>

	<div class="tab-content" id="tab">
	</div>

	<script>
		document.querySelectorAll('.tab-btn').forEach(btn => {
			btn.addEventListener('click', () => {
				document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
				document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));

				btn.classList.add('active');
				document.getElementById(btn.dataset.tab)?.classList.add('active');
			});
		});
	</script>
</Base>
